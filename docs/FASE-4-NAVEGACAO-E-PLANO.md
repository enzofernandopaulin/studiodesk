# Fase 4 — navegação e plano ativo

A rota lógica antiga `client_detail` continua aceita como alias durante a transição e agora renderiza e carrega o mesmo módulo de `client_profile`. O plano exibido vem do workspace ativo retornado pelo servidor, não do perfil editável. A equipe aparece em todos os planos com múltiplos usuários. A contagem de agenda usa a data local do navegador.
